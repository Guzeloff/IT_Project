@model List<IT_PROJECT_OnlineStore.Models.Category>

@{
    ViewBag.Title = "Index";
}

<h2>Browse Category</h2>
<p>
	Select from @Model.Count();

</p>
<ul>
	@foreach (var c in Model)
	{
		<li>@Html.ActionLink(c.Name, "Browse", new { c = c.Name })</li>
	}
</ul>